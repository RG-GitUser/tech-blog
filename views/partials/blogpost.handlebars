<html>
<body>
<!-- Single Blog Post Handlebars Template -->
<div id="single-post-template">
  <h2>{{blogPost.title}}</h2>
  <p>{{blogPost.description}}</p>
  <p>Posted by {{blogPost.user_id}} on {{blogPost.dateCreated}}</p>

  <div id="comments">
   
    <ul>
      {{#each blogPost.comments}}
        <li>{{this}}</li>
      {{/each}}
    </ul>
  </div>

  <form id="comment-form">
    <label for="comment">Leave a comment:</label>
    <textarea id="comment" name="comment" rows="4" cols="50"></textarea>
    <button type="submit">Submit Comment</button>
  </form>
</div>

{{!-- Content of the blog post view --}}
{{> blogContainer blogPostContent=blogPost commentsContent=comments}}


<script src="https://cdn.jsdelivr.net/npm/handlebars@4.7.7/dist/handlebars.min.js"></script>
<script src="./js/home.js"></script>
</body>
</html>
